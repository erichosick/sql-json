var pkg = require('../package.json');

describe("the modules were built correctly", function() {
  // it("01: should have the correct version", function() {
  //   expect(sqljsonlib["version-" + pkg.namesub]).to.equal("0.0.1");
  // });
});